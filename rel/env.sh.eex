#!/bin/sh

# configure node for distributed erlang
# Use "sname" (short names) for single-node Docker deployments
# Use "name" (long names) for distributed clustering (requires DNS resolution)
export RELEASE_DISTRIBUTION="${RELEASE_DISTRIBUTION:-sname}"
